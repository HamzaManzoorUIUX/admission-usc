import { Button } from "@mui/material";

const App = () => {
  return (
    <div>
      <Button variant="contained" color="primary" className="mt-4">
        Material UI  2
      </Button>
    </div>
  );
};

export default App;
